import { describe, it, expect } from 'vitest'
// import { setup, $fetch } from '@nuxt/test-utils'

describe('Test utils', async () => {
  it('can run a dummy test', () => {
    expect(true).to.be.true
  })
})
